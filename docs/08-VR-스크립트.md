# VR 스크립트 사용법

Quest 3에서 사용할 수 있는 VR 전용 스크립트들이에요!

---

## Quest3Controller (컨트롤러 진동)

컨트롤러를 진동시키는 기능이에요. "햅틱 피드백"이라고도 해요.

### 왜 필요할까요?

- 물체를 잡을 때 "찰칵" 느낌
- 문을 열 때 "쿵" 느낌
- 공포 연출에서 심장 박동 느낌

### Unity에서 설정하기

1. 컨트롤러 오브젝트 선택 (Left Controller 또는 Right Controller)
2. **Add Component** > `Quest3Controller` 추가

### 코드에서 사용하기

```csharp
Quest3Controller controller = GetComponent<Quest3Controller>();

// 기본 진동 (짧게 진동)
controller.Vibrate();

// 클릭 느낌 (아주 짧게)
controller.VibrateClick();

// 충돌 느낌 (강하고 짧게)
controller.VibrateImpact();

// 커스텀 진동 (강도, 시간)
controller.Vibrate(0.5f, 0.2f);  // 50% 강도로 0.2초
```

### 예시: 물체 잡을 때 진동

```csharp
public class MyGrabbable : XRGrabInteractable
{
    protected override void OnSelectEntered(SelectEnterEventArgs args)
    {
        base.OnSelectEntered(args);

        // 잡은 컨트롤러에서 진동
        var controller = args.interactorObject.transform
            .GetComponent<Quest3Controller>();

        if (controller != null)
        {
            controller.VibrateClick();
        }
    }
}
```

---

## QuestLinkManager (연결 관리)

Quest Link 연결 상태를 확인하고 관리해요.

### Unity에서 설정하기

1. 빈 오브젝트 생성
2. **Add Component** > `QuestLinkManager` 추가

### 코드에서 사용하기

```csharp
// 연결됐을 때 실행할 코드 등록
QuestLinkManager.Instance.OnVRConnected.AddListener(() => {
    Debug.Log("VR 헤드셋 연결됨!");
});

// 연결 끊겼을 때
QuestLinkManager.Instance.OnVRDisconnected.AddListener(() => {
    Debug.Log("VR 헤드셋 연결 끊김!");
});

// 현재 연결 상태 확인
if (QuestLinkManager.Instance.IsVRConnected) {
    Debug.Log("VR 사용 중");
} else {
    Debug.Log("데스크톱 모드");
}
```

---

## Quest3HandTracking (핸드 트래킹)

컨트롤러 없이 맨손으로 조작하는 기능이에요.

### 핀치란?

엄지와 검지를 붙이는 동작이에요. "집기" 또는 "선택"에 사용해요.

```
     👆 (검지)
      │
      │  ← 이렇게 붙이면 "핀치"
      │
     👍 (엄지)
```

### Unity에서 설정하기

1. XR Origin에 **Quest3HandTracking** 스크립트 추가
2. Project Settings > XR Plug-in Management > OpenXR에서
   - **Hand Tracking Subsystem** 체크

### 코드에서 사용하기

```csharp
Quest3HandTracking handTracking = GetComponent<Quest3HandTracking>();

// 핀치 시작할 때
handTracking.OnPinchStart.AddListener((hand) => {
    Debug.Log($"{hand} 손 핀치 시작!");
});

// 핀치 끝날 때
handTracking.OnPinchEnd.AddListener((hand) => {
    Debug.Log($"{hand} 손 핀치 끝!");
});

// 현재 핀치 상태 확인
if (handTracking.IsRightPinching) {
    Vector3 position = handTracking.RightPinchPosition;
    Debug.Log($"오른손 핀치 위치: {position}");
}
```

---

## GrabbableObject (잡을 수 있는 물체)

물체를 쉽게 잡을 수 있게 해주는 스크립트예요.

### Unity에서 설정하기

1. 잡을 물체 선택
2. **Add Component** > `GrabbableObject` 추가
3. 끝! (Rigidbody, Collider 등이 자동 추가돼요)

### 자동으로 추가되는 것들

- **Rigidbody**: 물리 법칙 적용
- **Box Collider** (없으면): 충돌 감지
- **XR Grab Interactable**: VR에서 잡기 기능

### 추가 설정

| 설정 | 설명 |
|------|------|
| Use Gravity | 중력 적용 여부 |
| Throw On Detach | 놓을 때 던지기 |
| Smooth Position | 부드러운 위치 이동 |

---

## VRButton (VR 버튼)

손으로 누를 수 있는 물리 버튼이에요.

### Unity에서 설정하기

1. 버튼 오브젝트 만들기 (Cube 등)
2. **Add Component** > `VRButton` 추가
3. Inspector에서 `On Button Pressed` 이벤트에 동작 연결

### 버튼 이벤트 연결하기

1. `On Button Pressed` 옆의 **+** 버튼 클릭
2. 실행할 오브젝트를 드래그 앤 드롭
3. 실행할 함수 선택

### 예시: 버튼 누르면 문 열기

1. 문 오브젝트를 On Button Pressed에 드래그
2. 드롭다운에서 **Door** > **OpenDoor** 선택
3. 버튼 누르면 문이 열려요!

---

## SnapZone (스냅 영역)

물체를 특정 위치에 딱 맞게 놓는 영역이에요.

### 사용 예시

- 열쇠를 열쇠 구멍에 끼우기
- 배터리를 충전기에 넣기
- 퍼즐 조각 맞추기

### Unity에서 설정하기

1. 빈 오브젝트 생성 (스냅 위치)
2. **Add Component** > `SnapZone` 추가
3. `Accepted Tags`에 허용할 물체 태그 설정

### 예시: 열쇠 구멍

```csharp
public class KeyHole : SnapZone
{
    public Door door;

    protected override void OnObjectSnapped(GameObject obj)
    {
        // 열쇠가 끼워지면 문 열기
        if (obj.CompareTag("Key"))
        {
            door.Unlock();
            door.OpenDoor();
        }
    }
}
```

---

## TeleportArea (텔레포트 영역)

이 영역으로 순간이동할 수 있어요.

### Unity에서 설정하기

1. 바닥 오브젝트 선택
2. **Add Component** > `Teleportation Area` 추가
3. 끝!

### 설정 옵션

| 설정 | 설명 |
|------|------|
| Match Orientation | 텔레포트 후 방향 맞추기 |
| Teleport Trigger | 텔레포트 실행 조건 |

### 텔레포트 불가 영역 만들기

특정 영역을 텔레포트 못 하게 하려면:

1. 해당 영역에 **Teleportation Area**를 추가하지 않으면 돼요
2. 또는 Collider에 별도 레이어를 지정해서 제외

---

## VRHUD (VR UI)

VR에서 보이는 상태 표시 UI예요.

### 표시되는 정보

- 스태미나 바
- 배터리 바
- 열쇠 개수
- 상태 텍스트

### Unity에서 설정하기

1. 빈 오브젝트 생성
2. **Add Component** > `VRHUD` 추가
3. UI 요소들을 연결 (Stamina Bar, Battery Bar 등)

### 코드에서 사용하기

```csharp
// 상태 텍스트 표시
VRHUD.Instance.ShowStatus("열쇠를 찾았습니다!", 3f);

// 경고 표시
VRHUD.Instance.ShowWarning("배터리가 부족합니다!");
```

---

## 다음 단계

VR 스크립트를 배웠어요! Quest 3 컨트롤러를 자세히 알아봅시다.

[다음: 컨트롤러 가이드 >](09-컨트롤러-가이드.md)
